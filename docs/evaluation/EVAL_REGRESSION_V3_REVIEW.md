# 评测集 v3 冻结记录

状态：`frozen_v3_holdout`

## 结论

- 用户已逐组确认 30 道问题，全部标记为 `keep`。
- 分布为 Simulink、AUTOSAR、Stateflow、Testing、Coverage、跨领域各 5 道。
- 24 道知识题已采用 source-first 方法标注；标注过程未运行 BM25、Dense、Wiki、图谱或 reranker。
- 6 道边界题不绑定证据块，用于检查库外问题、私有材料缺失与不充分结论。
- 76 个唯一 Gold evidence 均在 SQLite 中存在，且文档归属与题目声明一致。

## 冻结指纹

`EVAL_REGRESSION_V3.json` SHA-256：

`bded80c0c594d73c0d9b763397e31f7648d24b11a884fdbeb6a22e9b161f94e7`

## 使用约束

1. 重构前只保存一次盲基线，不根据该结果修改题目或 Gold。
2. 查询扩展与多标签路由重构完成后，只做一次正式 v3 验收。
3. 如果根据 v3 结果继续调代码、阈值、同义词或提示词，v3 立即降级为开发集，下一次正式验收必须新建 v4。
4. 日常迭代继续使用 v2 或专门的开发集，不能反复跑 v3 追分。

## 已知设计重点

- AUTOSAR 问题保留原问题中的多意图权重，避免只靠扩展词覆盖。
- 间接流程、口语表达与缺失材料使用自然改写，避免枚举固定句式。
- 跨域题允许多个领域标签和多个来源文档，不强制归入单一路由。
